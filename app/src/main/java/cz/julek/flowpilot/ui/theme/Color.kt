package cz.julek.flowpilot.ui.theme

import androidx.compose.ui.graphics.Color

// ── FlowPilot Brand Colors ──

// Primary — Deep indigo (focus, discipline)
val FlowPilotPrimary = Color(0xFF1A237E)
val FlowPilotOnPrimary = Color(0xFFFFFFFF)

// Secondary — Amber/gold (alerts, warnings)
val FlowPilotSecondary = Color(0xFFFF8F00)
val FlowPilotOnSecondary = Color(0xFF000000)

// Tertiary — Teal (success, connected)
val FlowPilotTertiary = Color(0xFF00695C)
val FlowPilotOnTertiary = Color(0xFFFFFFFF)

// Error — Red (intervention, danger)
val FlowPilotError = Color(0xFFD32F2F)
val FlowPilotOnError = Color(0xFFFFFFFF)

// Backgrounds
val FlowPilotBackgroundLight = Color(0xFFFAFAFA)
val FlowPilotSurfaceLight = Color(0xFFFFFFFF)
val FlowPilotBackgroundDark = Color(0xFF0D1117)
val FlowPilotSurfaceDark = Color(0xFF161B22)

// Status colors
val StatusConnected = Color(0xFF2E7D32)
val StatusDisconnected = Color(0xFFC62828)
val StatusConnecting = Color(0xFF1565C0)
val StatusWarning = Color(0xFFE65100)
